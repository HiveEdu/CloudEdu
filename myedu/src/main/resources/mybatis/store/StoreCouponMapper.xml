<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myedu.project.store.mapper.StoreCouponMapper">
    
    <resultMap type="StoreCoupon" id="StoreCouponResult">
        <result property="id"    column="id"    />
        <result property="title"    column="title"    />
        <result property="integral"    column="integral"    />
        <result property="couponPrice"    column="coupon_price"    />
        <result property="useMinPrice"    column="use_min_price"    />
        <result property="couponTime"    column="coupon_time"    />
        <result property="sort"    column="sort"    />
        <result property="status"    column="status"    />
        <result property="addTime"    column="add_time"    />
        <result property="isDel"    column="is_del"    />
    </resultMap>

    <sql id="selectStoreCouponVo">
        select id, title, integral, coupon_price, use_min_price, coupon_time, sort, status, add_time, is_del from store_coupon
    </sql>

    <select id="selectStoreCouponList" parameterType="StoreCoupon" resultMap="StoreCouponResult">
        <include refid="selectStoreCouponVo"/>
        <where>  
            <if test="title != null  and title != ''"> and title like concat('%', #{title}, '%')</if>
        </where>
    </select>
    
    <select id="selectStoreCouponById" parameterType="Long" resultMap="StoreCouponResult">
        <include refid="selectStoreCouponVo"/>
        where id = #{id}
    </select>
        
    <insert id="insertStoreCoupon" parameterType="StoreCoupon" useGeneratedKeys="true" keyProperty="id">
        insert into store_coupon
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="title != null  and title != ''">title,</if>
            <if test="integral != null ">integral,</if>
            <if test="couponPrice != null ">coupon_price,</if>
            <if test="useMinPrice != null ">use_min_price,</if>
            <if test="couponTime != null ">coupon_time,</if>
            <if test="sort != null ">sort,</if>
            <if test="status != null ">status,</if>
            <if test="addTime != null ">add_time,</if>
            <if test="isDel != null ">is_del,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="title != null  and title != ''">#{title},</if>
            <if test="integral != null ">#{integral},</if>
            <if test="couponPrice != null ">#{couponPrice},</if>
            <if test="useMinPrice != null ">#{useMinPrice},</if>
            <if test="couponTime != null ">#{couponTime},</if>
            <if test="sort != null ">#{sort},</if>
            <if test="status != null ">#{status},</if>
            <if test="addTime != null ">#{addTime},</if>
            <if test="isDel != null ">#{isDel},</if>
         </trim>
    </insert>

    <update id="updateStoreCoupon" parameterType="StoreCoupon">
        update store_coupon
        <trim prefix="SET" suffixOverrides=",">
            <if test="title != null  and title != ''">title = #{title},</if>
            <if test="integral != null ">integral = #{integral},</if>
            <if test="couponPrice != null ">coupon_price = #{couponPrice},</if>
            <if test="useMinPrice != null ">use_min_price = #{useMinPrice},</if>
            <if test="couponTime != null ">coupon_time = #{couponTime},</if>
            <if test="sort != null ">sort = #{sort},</if>
            <if test="status != null ">status = #{status},</if>
            <if test="addTime != null ">add_time = #{addTime},</if>
            <if test="isDel != null ">is_del = #{isDel},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteStoreCouponById" parameterType="Long">
        delete from store_coupon where id = #{id}
    </delete>

    <delete id="deleteStoreCouponByIds" parameterType="String">
        delete from store_coupon where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    
</mapper>